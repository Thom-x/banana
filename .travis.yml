sudo: required

services:
  - docker

language: java

before_install:
  - sudo apt-get update
  - sudo apt-get -y -o Dpkg::Options::="--force-confnew" install docker-ce
  - docker build .

script:
  - mvn clean install -B